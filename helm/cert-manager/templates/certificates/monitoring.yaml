apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: monitoring-cert
  namespace: monitoring
spec:
  secretName: monitoring-tls
  issuerRef:
    name: homelab-issuer
    kind: ClusterIssuer
  dnsNames:
    - grafana.lab
    - prometheus.lab
    - alertmanager.lab